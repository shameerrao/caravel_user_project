# Python dependencies for Caravel user project (RTL-to-GDS, verification, precheck).
# Install with: pip install -r requirements.txt
# PDK version manager used by Makefile targets like `pdk-with-ciel`
ciel
